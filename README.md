An ai coding assistant designed to read files, locate errors, write the fixes into your code and then run it to check that it works.

Currently set up to use the calculator project as the working directory but can be altered to any arbitrary project directory.